#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: C program that prints the alphabets in lower case
 *
 * Return: 0 (correct)
 */

int main(void)
{
	int start = 97;
	int end = 122;

	while (start < end + 1)
	{
		putchar(start);
		i++;
	}
	return (0);
}
